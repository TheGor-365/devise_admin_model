<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand me-5" href="/">Devise Admin Model</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse ms-5" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= root_path %>">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= users_path %>">Users</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= admin_path %>">Admin</a>
        </li>
      </ul>
    </div>
    <% if user_signed_in? %>
      <small class="border rounded border-success p-1 me-3 text-success">
        <%= current_user.email %>
      </small>
      <%= link_to 'User Settings', edit_user_registration_path, class: "text-dark text-decoration-none me-2" %>
      <%= link_to 'User Sign out', destroy_user_session_path, method: :delete, class: "btn btn-outline-danger" %>
    <% else %>
      <%= link_to 'User Sign in', new_user_session_path, class: "btn btn-success me-2" %>
      <%= link_to 'User Sign up', new_user_registration_path, class: "btn btn-secondary" %>
    <% end %>
    <% if admin_signed_in? %>
      <small class="border rounded border-success p-1 me-3 text-success">
        <%= current_admin.email %>
      </small>
      <%= link_to 'Admin Settings', edit_admin_registration_path, class: "text-dark text-decoration-none me-2" %>
      <%= link_to 'Admin Sign out', destroy_admin_session_path, method: :delete, class: "btn btn-outline-danger" %>
    <% else %>
      <%= link_to 'Admin Sign in', new_admin_session_path, class: "btn btn-success me-2" %>
      <%= link_to 'Admin Sign up', new_admin_registration_path, class: "btn btn-secondary" %>
    <% end %>
  </div>
</nav>
